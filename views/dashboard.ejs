<section id="dashboard">

    <div id="schedule">
        <% schedules.forEach(schedule => { %>
        <p><%= schedule.day %>-<%= schedule.month %>-<%= schedule.year %></p>
        <% }) %>
    </div>

</section>